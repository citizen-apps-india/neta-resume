# Python
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pulumi/

# uv
# (uv.lock IS committed; this is only caches)

# Ingestion raw cache & secrets (provenance snapshots are large + may carry PII)
ingestion/data/raw_cache/
# Generated per-cycle candidate indexes + Tier-2 review queues (regenerable, large)
ingestion/data/hist_index/
*.sqlite
*.db

# Node / Next.js
node_modules/
web/.next/
web/out/
web/.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Pulumi local artifacts (stack configuration is reviewed and committed separately)
infra/.pulumi/
infra/bootstrap/.pulumi/

# OS / editor
.DS_Store
.idea/
.vscode/
*.log

# API photo proxy cache
api/.photo_cache/

# raw provenance cache when a pipeline is run from the repo root (workspace)
/data/raw_cache/
api/.doc_cache/
